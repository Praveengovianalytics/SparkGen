## SparkGen Default Context

- Target: YAML-driven RAG + agent workflows.
- Knowledge Bases: reference `product_docs` for orchestration details and `responsible_ai` for safety posture.
- Guardrails: avoid leaking secrets, cite sources, keep steps reproducible.
- Preferred outputs: bullet points with tool call rationale and explicit KB references.
